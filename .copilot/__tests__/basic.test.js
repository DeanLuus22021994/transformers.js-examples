import { describe, expect, test } from '@jest/globals';

describe('Workspace environment', () => {
  test('Basic test to verify Jest configuration works', () => {
    expect(1 + 1).toBe(2);
  });
});
